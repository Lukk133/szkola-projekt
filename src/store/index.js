import { createStore } from 'vuex'

export default createStore({
      state: {
        showAlert: false,
        visible: false,
  },
  getters: {
  },
  mutations: {
  },
  actions: {},
  modules: {
  }
})
